template:
  root: ./template
  serviceClass: ameba.aeromock.template.jade4j.Jade4jTemplateService

data:
  root: ../data

static:
  root: ../static

ajax:
  root: ../json/ajax

function:
  root: ./function
