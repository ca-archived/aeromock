template:
  root: ./template
  serviceClass: jp.co.cyberagent.aeromock.template.jade4j.Jade4jTemplateService

data:
  root: ../data

static:
  root: ../static

ajax:
  root: ../json/ajax

function:
  root: ./function
