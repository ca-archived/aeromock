template:
  root: ./template
  serviceClass: jp.co.cyberagent.aeromock.template.thymeleaf.ThymeleafTemplateService

data:
  root: ../data

static:
  root: ../static

ajax:
  root: ../json/ajax

function:
  root: ./function
