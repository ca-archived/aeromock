package ameba.aeromock.template.handlebars

import ameba.aeromock.core.bootstrap.Bootstrap

/**
 *
 * @author stormcat24
 */
class HandlebarsBootstrap extends Bootstrap {

  /**
   * @inheritdoc
   */
  override def process(): Unit = {
    // nothing to do.
  }
}
